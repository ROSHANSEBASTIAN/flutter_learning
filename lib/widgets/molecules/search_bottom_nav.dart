import 'package:flutter/material.dart';

class SearchBottomNav extends StatelessWidget {
  const SearchBottomNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Search"),
    );
  }
}
